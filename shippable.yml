build:
  ci:
    - docker build . -t tolutest22/portfolio:$BUILD_NUMBER 
    - docker tag tolutest22/portfolio:$BUILD_NUMBER tolutest22/portfolio:latest
    - docker push tolutest22/portfolio:$BUILD_NUMBER
    - docker push tolutest22/portfolio:latest
  
integrations:
  hub:
    - integrationName: docker-integration    
      type: dockerRegistryLogin
